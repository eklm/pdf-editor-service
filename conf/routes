# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET      /pdfeditor/generated_pdfs/*filename  controllers.PdfEditor.getPdf(filename)

POST     /pdfeditor   controllers.PdfEditor.generatePdf()
